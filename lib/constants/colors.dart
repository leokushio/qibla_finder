import 'package:flutter/material.dart';

class XColors {

  // -- Primary Colors
  static const Color primary = Color(0xFF464E60);
  static const Color secondary = Color(0xFFF5EFD2);

  // Gradient Colors
  static const Gradient bgGradient = LinearGradient(
    begin: Alignment(0, -1),
    end: Alignment(0, 0.8),
    colors: [
      Color(0xff464E60),
      Color(0xff90A1C6),
  ]);
}